# CRC Release Safety Reports

This directory contains release safety summaries built from CRC harness runs.

Each report records the change type, version chain, referenced harness runs,
hard-fail summary, release decision, and rollback target. Artifacts contain only
structured case identifiers, expected/actual outcomes, version labels, release
decisions, and rollback metadata.
